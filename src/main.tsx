import React from 'react'
import ReactDOM from 'react-dom/client'
import './main.css'
import WebRouting from './router/WebRouting'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <WebRouting />
  </React.StrictMode>,
)
